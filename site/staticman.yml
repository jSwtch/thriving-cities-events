comments:
  allowedFields: ["name", "email", "message"]
  allowedOrigins: ["localhost", "heuristic-lalande-8c9f8f.netlify.com"]
  branch: "master"
  commitMessage: "Add Staticman data"
  filename: "entry{@timestamp}"
  format: "yaml"
  generatedFields:
    date:
      type: date
      options:
        format: "timestamp-seconds"
  moderation: true
  name: "Thriving Cities"
  path: "_data/comments/{options.slug}"
  requiredFields: ["name", "email", "message"]
  transforms:
    email: md5




